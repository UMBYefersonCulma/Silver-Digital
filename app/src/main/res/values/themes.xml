<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema Claro -->
    <style name="Theme.SilverDigital.Light" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorTertiary">@color/tertiary_color</item>

        <!-- Fondo y texto -->
        <item name="android:colorBackground">@color/background_color</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>

        <!-- Colores para botones -->
        <item name="buttonStyle">@style/ButtonStyle</item>
    </style>

    <!-- Tema Oscuro -->
    <style name="Theme.SilverDigital.Dark" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorTertiary">@color/tertiary_color</item>

        <!-- Fondo y texto -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/gray</item>

        <!-- Colores para botones -->
        <item name="buttonStyle">@style/ButtonStyle</item>
    </style>
</resources>